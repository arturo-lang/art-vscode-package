name: Extension

on:
    pull_request:
        branches:
            - main
            - master
            - develop

jobs:
    validate:
        runs-on: ubuntu-latest
        
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
            
            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '20'
            
            - name: Install vsce
              run: npm install -g @vscode/vsce
            
            - name: Validate extension package
              id: validate
              run: |
                  set +e  # Don't exit on error
                  vsce package --no-git-tag-version > vsce_stdout.log 2> vsce_stderr.log
                  EXIT_CODE=$?
                  cat vsce_stdout.log vsce_stderr.log > vsce_output.log
                  
                  echo "=== VSCE Exit Code: $EXIT_CODE ==="
                  echo "=== Contents of vsce_output.log ==="
                  cat vsce_output.log
                  echo "=== End of log ==="
                  
                  exit $EXIT_CODE
              continue-on-error: true
            
            - name: List generated package
              if: steps.validate.outcome == 'success'
              run: |
                  if ls *.vsix 1> /dev/null 2>&1; then
                      ls -lh *.vsix
                  fi
            
            - name: Comment on PR with error
              if: steps.validate.outcome == 'failure'
              uses: actions/github-script@v7
              with:
                  script: |
                      const fs = require('fs');
                      const output = fs.readFileSync('vsce_output.log', 'utf8');
                      
                      // Try multiple patterns to extract the error
                      let errorMsg = 'Unknown error occurred';
                      
                      // Pattern 1: Look for lines with ERROR (case insensitive)
                      const lines = output.split('\n');
                      for (const line of lines) {
                          if (line.match(/ERROR/i) || line.match(/Error:/)) {
                              // Clean up the line - remove ANSI codes, leading symbols, etc
                              errorMsg = line
                                  .replace(/\x1b\[[0-9;]*m/g, '') // Remove ANSI codes
                                  .replace(/^.*?ERROR\s*/i, '')    // Remove everything up to and including ERROR
                                  .replace(/^.*?Error:\s*/i, '')   // Remove everything up to and including Error:
                                  .trim();
                              if (errorMsg) break;
                          }
                      }
                      
                      github.rest.issues.createComment({
                          issue_number: context.issue.number,
                          owner: context.repo.owner,
                          repo: context.repo.repo,
                          body: `❌ **Extension validation failed**\n\n\`\`\`\n${errorMsg}\n\`\`\`\n\n[View full logs](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})`
                      })
            
            - name: Fail workflow if validation failed
              if: steps.validate.outcome == 'failure'
              run: exit 1